# Rules

- Do not execute any scripts or run commands in the terminal without explicit user permission. Propose commands using `run_command` but always ensure the user is aware and approves before executing.
